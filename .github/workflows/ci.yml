name: CI

# Test-only CI: builds and publishes nothing, and holds no repository credentials.
# Publishing stays with the separate script-first pipeline. All logic lives in ci/run_ci.py
# so the workflow is a thin
# wrapper. Actions are pinned to full commit SHAs (tag noted in the comment) so a moved tag
# cannot change what runs.

on: [push, pull_request]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: tests (${{ matrix.os }}, py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.12", "3.14"]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run cross-platform CI (import + version, pytest, smoke)
        # uv sync --locked installs the exact versions pinned in uv.lock (project +
        # duckdb + the dev-group pytest), failing if the lock is stale. The runner then
        # uses that locked venv, so CI is reproducible rather than re-resolving each run.
        run: |
          uv sync --locked
          uv run python ci/run_ci.py
